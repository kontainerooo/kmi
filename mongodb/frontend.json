{
	"imports": ["uploads", "ssh"],
	"modules":[
        {
	        "template": "status",
	        "parameters": {
	            "title": "MongoDB Status",
	            "start": "$KROO_CMD_START",
	            "stop": "$KROO_CMD_STOP",
	            "status": "$KROO_CMD_STATUS"
	        }
	    },
		{
			"template": "ssh",
			"parameters": {
				"title": "SSH",
				"publicKey": "./ssh.pub",
				"logfile": "./ssh.log"
			}
		}
	]
}
